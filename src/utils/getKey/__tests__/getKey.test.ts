/**
 * Utils - Get Key Test
 */

/* Imports */

import { it, expect, describe } from 'vitest'
import { getKey } from '../getKey'

/* Tests */

describe('getKey()', () => {
  // should return empty string if key does not exist
  // should return ESC
  // should return SPACE
  // should return LEFT

  it('', () => {

  })
})

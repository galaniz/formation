# Html

## isHtmlElement  

**<code>isHtmlElement(value: &ast;, instance?: HTMLElement): boolean</code>**  

Check if value is an HTMLElement.

### Parameters  
- **`value`** <code>&ast;</code> required  
- **`instance`** <code>HTMLElement</code> optional

### Returns  

<code>boolean</code>

## isHtmlElementArray  

**<code>isHtmlElementArray(value: &ast;, instance?: HTMLElement): boolean</code>**  

Check if first value in array is HTMLElement.

### Parameters  
- **`value`** <code>&ast;</code> required  
- **`instance`** <code>HTMLElement</code> optional

### Returns  

<code>boolean</code>

## isHtmlElementArrayStrict  

**<code>isHtmlElementArrayStrict(value: &ast;, instance?: HTMLElement): boolean</code>**  

Check if all values in array are HTMLElement.

### Parameters  
- **`value`** <code>&ast;</code> required  
- **`instance`** <code>HTMLElement</code> optional

### Returns  

<code>boolean</code>